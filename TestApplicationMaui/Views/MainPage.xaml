<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tb="clr-namespace:TabViewMaui;assembly=TabViewMaui"
             xmlns:vp="clr-namespace:ViewPagerMaui;assembly=ViewPagerMaui"
             xmlns:rsInput="clr-namespace:RSInputViewMaui;assembly=RSInputViewMaui"
             xmlns:vm="clr-namespace:TestApplicationMaui.ViewModels"
             xmlns:views="clr-namespace:TestApplicationMaui.Views"
             x:Class="TestApplicationMaui.Views.MainPage">

    <ContentPage.BindingContext>
        <vm:MainViewModel/>
    </ContentPage.BindingContext>
    <VerticalStackLayout x:Name="stack" Margin="10" Spacing="20">

        <Entry Placeholder="Stock Entry" />

        <Grid RowDefinitions="Auto" ColumnDefinitions="*, *" ColumnSpacing="15" BackgroundColor="Green">
            <rsInput:RSInputView Grid.Row="0" Grid.Column="0" Design="Filled" BackgroundColor="Pink" Placeholder="Label">
                <Entry />
            </rsInput:RSInputView>

            <rsInput:RSInputView Grid.Row="0" Grid.Column="1" BackgroundColor="Pink" Placeholder="Label">
                <Entry/>
            </rsInput:RSInputView>
        </Grid>

        <!--<rsInput:RSInputView  TrailingIcon="dotnet_bot.png" Placeholder="Label">
            <Entry/>
        </rsInput:RSInputView>

        <Grid RowDefinitions="Auto" ColumnDefinitions="*, *, *" ColumnSpacing="15" BackgroundColor="Green">
            <rsInput:RSInputView Grid.Row="0" Grid.Column="0" Design="Filled" BackgroundColor="Pink" Placeholder="Label">
                <Entry />
            </rsInput:RSInputView>

            <rsInput:RSInputView Grid.Row="0" Grid.Column="1" BackgroundColor="Pink" Placeholder="Label">
                <Entry/>
            </rsInput:RSInputView>

            <Entry Grid.Row="0" Grid.Column="2" BackgroundColor="Pink"/>
        </Grid>

        <Grid RowDefinitions="Auto" ColumnDefinitions="*" ColumnSpacing="15" BackgroundColor="Green" Padding="5, 0, 5, 0">
            <rsInput:RSInputView Grid.Row="0" Grid.Column="0" BackgroundColor="Pink" Helper="wefwewef" Placeholder="Label">
                <Entry/>
            </rsInput:RSInputView>
        </Grid>-->


        <Button Text="Add Error" Clicked="Button_Clicked"/>

        <Button Text="Remove Error" Clicked="Button_Clicked2"/>

        <Label Text="{Binding Error}" TextColor="Green"/>

    </VerticalStackLayout>
</ContentPage>
